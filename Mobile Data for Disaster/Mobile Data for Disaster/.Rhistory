datasets_dir = "E:/Datasets/Mobile-Data-for-Disaster/"; datasets_dir = "C:/Datasets/"; lists = list.files(datasets_dir); for (i in 1:length(lists)) {     loc_file = paste(datasets_dir,lists[i],sep = "");     if (i==1) {         output = read.csv(loc_file,stringsAsFactors=FALSE);     } else {         dat = read.csv(loc_file, stringsAsFactors = FALSE);         output = rbind(output,dat);     } }
datasets_dir = "C:/Datasets/"; lists = list.files(datasets_dir); for (i in 1:length(lists)) {     loc_file = paste(datasets_dir,lists[i],sep = "");     if (i==1) {         output = read.csv(loc_file,stringsAsFactors=FALSE);     } else {         dat = read.csv(loc_file, stringsAsFactors = FALSE);         output = rbind(output,dat);     } }
datasets_dir = "C:/Datasets/Mobile-Data-for-Disaster/"; lists = list.files(datasets_dir); for (i in 1:length(lists)) {     loc_file = paste(datasets_dir,lists[i],sep = "");     if (i==1) {         output = read.csv(loc_file,stringsAsFactors=FALSE);     } else {         dat = read.csv(loc_file, stringsAsFactors = FALSE);         output = rbind(output,dat);     } }
colnames(output)
str(dat)
dat = output;
str(dat)
dat_filt = dat[is.na(dat$reporting__number_of_records),];
nrow(dat_filt)
nrow(dat)
dat_filt = dat[!is.na(dat$reporting__number_of_records),];
nrow(dat_filt)
dat_isna = dat[is.na(dat$reporting__number_of_records),];
str(dat_isna)
datasets_dir = "C:/Datasets/Mobile-Data-for-Disaster/"; lists = list.files(datasets_dir); for (i in 1:length(lists)) {     loc_file = paste(datasets_dir,lists[i],sep = "");     if (i==1) {         output = read.csv(loc_file, stringsAsFactors = FALSE);         output$week_type = lists[i];     } else {         dat = read.csv(loc_file, stringsAsFactors = FALSE);         dat$week_type = lists[i];         output = rbind(output,dat);     } } dat = output;
str(dat_filt)
head(dat)
lists
write.csv(dat_filt,"week-43-52-result.csv");
